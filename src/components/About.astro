---
import { Image } from 'astro:assets';
const mainJpg = '/photo-about-main.jpg';
import sareeJpg     from '../assets/images/photo_about saree.jpg';
import billboardJpg from '../assets/images/photo_about billboard.jpg';
import docJpg       from '../assets/images/photo_about doc.jpg';
import divingJpg    from '../assets/images/photo_about diving.jpg';
import podcastJpg   from '../assets/images/photo_about podcast.jpg';

const logos = [
  { src: '/logos/forbes.svg',                   alt: 'Forbes India',               w: 120 },
  { src: '/logos/deccan_herald.svg',            alt: 'Deccan Herald',              w: 170 },
  { src: '/logos/delhi.svg',                    alt: 'Government of Delhi', sq: true, w: 40 },
  { src: '/logos/natgeo.svg',                   alt: 'National Geographic',        w: 120 },
  { src: '/logos/gates.svg',                    alt: 'Gates Foundation',           w: 120 },
  { src: '/logos/cosmo.svg',                    alt: 'Cosmopolitan India',         w: 120 },
  { src: '/logos/hindustan_times.svg',          alt: 'Hindustan Times', sq: true,  w: 40  },
  { src: '/logos/vogue.svg',                    alt: 'Vogue India',                w: 120 },
  { src: '/logos/india_govt.svg',               alt: 'Ministry of Health',         w: 100 },
  { src: '/logos/umich.svg',                    alt: 'University of Michigan',     w: 120 },
  { src: '/logos/livemint.svg',                 alt: 'Live Mint',                  w: 70  },
  { src: '/logos/grihshobha.svg',               alt: 'Grihshobha',                 w: 150 },
  { src: '/logos/who.svg',                      alt: 'World Health Organization',  w: 120 },
  { src: '/logos/mumbai_mirror.svg',            alt: 'Mumbai Mirror',              w: 150 },
  { src: '/logos/the_hindu_illustrations.svg',  alt: 'The Hindu',                  w: 240 },
  { src: '/logos/british_vogue.svg',            alt: 'British Vogue',              w: 120 },
  { src: '/logos/ndtv.svg',                     alt: 'NDTV',                       w: 120 },
  { src: '/logos/national_herald.svg',          alt: 'National Herald',            w: 120 },
  { src: '/logos/times_of_india.svg',           alt: 'Times of India',             w: 120 },
  { src: '/logos/dainik_jagran.svg',            alt: 'Dainik Jagran',              w: 120 },
  { src: '/logos/mint_lounge.svg',              alt: 'Mint Lounge',                w: 120 },
  { src: '/logos/cnn_swades.svg',               alt: 'CNN Swades',                 w: 120 },
  { src: '/logos/cnn.svg',                      alt: 'CNN',                        w: 120 },
  { src: '/logos/the_week.svg',                 alt: 'The Week',                   w: 120 },
  { src: '/logos/brut.svg',                     alt: 'Brut',                       w: 120 },
  { src: '/logos/india_today.svg',              alt: 'India Today',                w: 150 },
  { src: '/logos/elle.svg',                     alt: 'Elle',                       w: 120 },
  { src: '/logos/the_economist.svg',            alt: 'The Economist',              w: 120 },
];

// Spread logos so similar widths are never adjacent.
// Greedy: always pick from the largest remaining group whose width differs from the previous.
const _groups = new Map<number, typeof logos>();
for (const logo of logos) {
  if (!_groups.has(logo.w)) _groups.set(logo.w, []);
  _groups.get(logo.w)!.push(logo);
}
const interleaved: typeof logos = [];
let _lastW = -1;
while (interleaved.length < logos.length) {
  let bestW = -1, bestCount = 0;
  for (const [w, g] of _groups) {
    if (w !== _lastW && g.length > bestCount) { bestW = w; bestCount = g.length; }
  }
  if (bestW === -1) bestW = _lastW; // forced: all remaining are same width
  const g = _groups.get(bestW)!;
  interleaved.push(g.shift()!);
  if (g.length === 0) _groups.delete(bestW);
  _lastW = bestW;
}

---

<section class="about" id="about">

  <!-- ─── SCENE: scales as one unit on mobile ─── -->
  <div class="about__scene">

    <!-- ─── PHOTO COLLAGE ─── -->
    <!-- main photo: CSS polaroid (white bg, shadow, bigger bottom border) -->
    <div class="about__photo about__photo--main">
      <img src={mainJpg} alt="Dr. Tanaya Narendra, known as Dr Cuterus, India's favourite sex educator and Oxford-trained doctor" class="about__photo-content" loading="eager" fetchpriority="high" />
    </div>

    <div class="about__photo about__photo--saree  about__polaroid about__polaroid--purple">
      <Image src={sareeJpg}     alt="Dr. Tanaya Narendra, Dr Cuterus, dressed in a saree" class="about__photo-content" loading="eager" width={441} height={551} />
    </div>
    <div class="about__photo about__photo--billboard about__polaroid about__polaroid--yellow">
      <Image src={billboardJpg} alt="Dr Cuterus featured on a public billboard campaign for sexual health awareness" class="about__photo-content" loading="eager" width={436} height={545} />
    </div>
    <div class="about__photo about__photo--doctor about__polaroid about__polaroid--purple">
      <Image src={docJpg}       alt="Dr. Tanaya Narendra in her role as a medical doctor" class="about__photo-content" loading="eager" width={435} height={543} />
    </div>
    <div class="about__photo about__photo--diving about__polaroid about__polaroid--purple">
      <Image src={divingJpg}    alt="Dr. Tanaya Narendra, Dr Cuterus, scuba diving" class="about__photo-content" loading="eager" width={441} height={551} />
    </div>
    <div class="about__photo about__photo--podcast about__polaroid about__polaroid--yellow">
      <Image src={podcastJpg}   alt="Dr. Tanaya Narendra recording The Dr. Cuterus Podcast" class="about__photo-content" loading="eager" width={809} height={420} />
    </div>


    <!-- "India's Favourite Sex Educator" badge -->
    <div class="about__badge" aria-hidden="true">
      <span class="about__badge-set about__badge-set--hinglish">
        <span>India ki Favourite</span>
        <span>Sex Educator</span>
      </span>
      <span class="about__badge-set about__badge-set--english">
        <span>India's Favourite</span>
        <span>Sex Educator</span>
      </span>
    </div>

    <!-- Stars — scattered across the collage -->
    <img src="/doodles/star about.svg" alt="" class="about__star about__star--1" aria-hidden="true" />
    <img src="/doodles/star about.svg" alt="" class="about__star about__star--2" aria-hidden="true" />
    <img src="/doodles/star about.svg" alt="" class="about__star about__star--3" aria-hidden="true" />
    <img src="/doodles/star about.svg" alt="" class="about__star about__star--4" aria-hidden="true" />

    <!-- ─── RIGHT-SIDE TEXT ─── -->

    <!-- Highlight bar sits behind bio text -->
    <div class="about__highlight-bar" aria-hidden="true"></div>

    <!-- Headlines -->
    <p class="about__headline about__headline--1">100% Science.</p>
    <p class="about__headline about__headline--2">0% Sharam.</p>

    <!-- +Thori si bakchodi pill -->
    <div class="about__pill-wrap" aria-hidden="true">
      <div class="about__pill-bakchodi">+Thori si bakchodi</div>
    </div>

    <!-- Bio — desktop (two-paragraph, right-aligned) -->
    <div class="about__bio-wrap about__bio-desktop">
      <div class="about__bio about__bio--hinglish">
        <p class="about__bio-intro">Hi, main hoon <strong>Dr Tanaya Narendra</strong>.<br/>AKA <strong>Dr Cuterus</strong>.</p>
        <p class="about__bio-body">Doctor, author, podcast host, educator, mai un sawaalon ke jawaab deti hoon jo poochne mein sabko sharam aati hai.</p>
      </div>
      <div class="about__bio about__bio--english">
        <p class="about__bio-intro">Hey, I'm <strong>Dr Tanaya Narendra</strong>.<br/>Better known as <strong>Dr Cuterus</strong>.</p>
        <p class="about__bio-body">A doctor, author, podcast host, educator, who answers questions people are too shy to ask out loud.</p>
      </div>
    </div>

    <!-- Bio — mobile (single paragraph, shorter, centred) -->
    <p class="about__bio-mobile about__bio-mobile--hinglish">
      Hi, main hoon <strong>Dr Tanaya Narendra</strong>.<br/>AKA <strong>Dr Cuterus</strong>.<br/>Doctor, author, podcast host, educator.
    </p>
    <p class="about__bio-mobile about__bio-mobile--english">
      Hey, I'm <strong>Dr Tanaya Narendra</strong>.<br/>Better known as <strong>Dr Cuterus</strong>.<br/>A doctor, author, podcast host, educator.
    </p>

    <!-- CTA -->
    <div class="about__cta-wrap">
      <a href="https://mail.google.com/mail/?view=cm&to=drcuterus@gmail.com" target="_blank" rel="noopener" class="btn-primary about__cta">Work With Me</a>
    </div>

    <!-- Scroll indicator -->
    <div class="about__scroll">
      <span>scroll for more</span>
      <svg class="about__scroll-arrow" width="12" height="10" viewBox="0 0 12 10" fill="none" xmlns="http://www.w3.org/2000/svg">
        <polyline points="1,1 6,8 11,1" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
      </svg>
    </div>

  </div><!-- /.about__scene -->

  <!-- ─── YELLOW TICKER (outside scene, always at bottom) ─── -->
  <div class="about__ticker" aria-label="Featured in">
    <div class="about__ticker-track">
      {interleaved.map(l => (
        <img
          src={l.src}
          alt={l.alt}
          class={`about__ticker-logo${l.sq  ? ' about__ticker-logo--sq'  : ''}${l.png ? ' about__ticker-logo--png' : ''}`}
        />
      ))}
      {interleaved.map(l => (
        <img
          src={l.src}
          alt=""
          class={`about__ticker-logo${l.sq  ? ' about__ticker-logo--sq'  : ''}${l.png ? ' about__ticker-logo--png' : ''}`}
        />
      ))}
    </div>
  </div>

</section>

<style>
  /* ─────────────────────────────────────────────
     SECTION
  ───────────────────────────────────────────── */
  .about {
    position: relative;
    height: 100svh;
    min-height: 560px;
    overflow: hidden;
    background-color: var(--color-beige-500);
  }

  /* ─────────────────────────────────────────────
     SCENE — fills section on desktop, scales on mobile
  ───────────────────────────────────────────── */
  .about__scene {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 60px; /* leave room for ticker */
  }

  /* ─────────────────────────────────────────────
     PHOTOS  (desktop — reference canvas 1280×832)
  ───────────────────────────────────────────── */
  .about__photo {
    position: absolute;
    display: block;
    height: auto;
    z-index: 2;
    -webkit-backface-visibility: hidden;
    backface-visibility: hidden;
  }

  /* JPEG fills its container */
  .about__photo-content {
    display: block;
    width: 100%;
    height: auto;
  }

  /* Polaroid card — small photos: thin top/sides, bigger bottom white strip */
  .about__polaroid {
    background: #fefefe;
    box-shadow: 0.4px 0.8px 2.5px 0.4px rgba(0,0,0,0.25);
    padding: 3px 3px 12px;
    overflow: visible;
  }

  /* coloured pin circle — centre sits exactly on the top edge (half inside, half outside) */
  .about__polaroid::before {
    content: '';
    position: absolute;
    width: 13px;
    height: 13px;
    border-radius: 50%;
    top: -6.5px;   /* half the height → centre on edge */
    left: 14%;
    z-index: 5;
    box-shadow: 0.3px 0.6px 1.5px rgba(0,0,0,0.5);
  }
  .about__polaroid--yellow::before { background: #FFD700; }
  .about__polaroid--purple::before { background: #6e3997; }

  .about__photo--main {
    width: 33%;
    max-width: 450px;
    left: 18.29%;
    top: 14.48%;
    transform: rotate(-1.87deg);
    transform-origin: center center;
    z-index: 2;
    /* CSS polaroid: thin top/sides, noticeably bigger bottom strip */
    background: #fefefe;
    box-shadow: 1.1px 2.2px 6.5px 1.1px rgba(0,0,0,0.25);
    padding: 9px 9px 38px;
  }

  .about__photo--saree {
    width: 10.61%;
    max-width: 140px;
    left: 7.31%;
    top: 18.45%;
    transform: rotate(3.25deg);
    z-index: 3;
  }

  .about__photo--billboard {
    width: 10.5%;
    max-width: 140px;
    left: 7.5%;
    top: 44%;
    transform: rotate(15.88deg);
    z-index: 1;
  }

  .about__photo--doctor {
    width: 12.82%;
    max-width: 168px;
    left: 12.2%;
    top: 65.02%;
    transform: rotate(9.68deg);
    z-index: 3;
  }

  .about__photo--diving {
    width: 12.46%;
    max-width: 165px;
    left: 49.42%;
    top: 28.44%;
    transform: rotate(-2.18deg);
    z-index: 4;
  }

  .about__photo--podcast {
    width: 19.73%;
    max-width: 258px;
    left: 49.79%;
    top: 63%;
    transform: rotate(8.09deg);
    z-index: 3;
  }

  /* ─────────────────────────────────────────────
     PHOTO LABELS
  ───────────────────────────────────────────── */
  .about__img-label {
    position: absolute;
    font-family: var(--font-sans);
    font-weight: 400;
    color: var(--color-brand-900);
    font-size: clamp(10px, 1.36vw, 17px);
    margin: 0;
    z-index: 6;
    white-space: nowrap;
    line-height: 1;
  }

  .about__img-label--rtfrb {
    left: 21.19%;
    top: 12.77%;
    transform: rotate(-2deg);
  }

  .about__img-label--btc {
    left: 53.95%;
    top: 75.58%;
    transform: rotate(15deg);
  }

  /* ─────────────────────────────────────────────
     "INDIA'S FAVOURITE SEX EDUCATOR" BADGE
  ───────────────────────────────────────────── */
  .about__badge {
    position: absolute;
    left: 28.55%;
    top: 75%;
    background: var(--color-brand-100);
    padding: 4px 8px;
    font-family: var(--font-sans);
    font-weight: 400;
    font-size: clamp(16px, 2.72vw, 28px);
    color: var(--color-brand-900);
    display: flex;
    line-height: 0.88;
    z-index: 5;
    white-space: nowrap;
  }

  .about__badge-set {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .about__badge-set--english { display: none; }
  :global([data-lang="english"]) .about__badge-set--english  { display: flex; }
  :global([data-lang="english"]) .about__badge-set--hinglish { display: none; }

  /* ─────────────────────────────────────────────
     STAR DECORATIONS
  ───────────────────────────────────────────── */
  .about__star {
    position: absolute;
    display: block;
    height: auto;
    pointer-events: none;
    z-index: 4;
  }

  .about__star--1 { width: 60px; left: 15.03%;  top: 15.7%; }
  .about__star--2 { width: 50px; left: 47.0%;  top: 28.0%; }
  .about__star--3 { display: none; }
  .about__star--4 { width: 64px; left: 47.5%;  top: 61.2%; }

  /* ─────────────────────────────────────────────
     HEADLINES
  ───────────────────────────────────────────── */
  .about__headline {
    position: absolute;
    margin: 0;
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    color: var(--color-brand-400);
    font-size: clamp(32px, 5vw, 64px);
    line-height: 1.3;
    letter-spacing: -0.03em;
    white-space: nowrap;
    z-index: 5;
    text-align: right;
  }

  .about__headline--1 {
    right: 4.69%;
    top: 19.47%;
  }
  .about__headline--2 {
    right: 3.28%;
    top: 28%;
  }

  /* ─────────────────────────────────────────────
     +THORI SI BAKCHODI PILL
  ───────────────────────────────────────────── */
  .about__pill-wrap {
    position: absolute;
    left: 80.13%;
    top: 40.25%;
    transform: rotate(-5.99deg);
  }
  .about__pill-bakchodi {
    background: var(--color-yellow-500);
    border-radius: 999px;
    padding: 8px 16px;
    font-family: var(--font-sans);
    font-weight: 400;
    font-size: clamp(14px, 1.875vw, 24px);
    color: var(--color-black);
    white-space: nowrap;
    line-height: 1.3;
  }

  /* ─────────────────────────────────────────────
     BIO TEXT
  ───────────────────────────────────────────── */
  .about__bio-wrap {
    position: absolute;
    right: 60px;
    left: auto;
    top: 49.55%;
    width: 25%;
    z-index: 5;
  }

  .about__bio {
    font-family: var(--font-sans);
    font-weight: 400;
    font-size: clamp(13px, 1.875vw, 20px);
    color: var(--color-brand-900);
    text-align: right;
    margin: 0;
  }

  .about__bio-intro {
    margin: 0;
    line-height: 1.15;
  }

  .about__bio-body {
    margin: 0.8em 0 0;
    line-height: 1.2;
  }

  .about__bio--english { display: none; }
  :global([data-lang="english"]) .about__bio--english  { display: block; }
  :global([data-lang="english"]) .about__bio--hinglish { display: none; }

  .about__bio-mobile { display: none; }

  /* ─────────────────────────────────────────────
     CTA BUTTON
  ───────────────────────────────────────────── */
  .about__cta-wrap {
    position: absolute;
    right: 60px;
    left: auto;
    top: 75.07%;
    z-index: 5;
  }

  .about__cta {
    font-size: clamp(14px, 1.875vw, 24px);
    padding: clamp(8px, 0.95vh, 12px) clamp(14px, 1.4vw, 18px);
  }

  /* ─────────────────────────────────────────────
     SCROLL INDICATOR
  ───────────────────────────────────────────── */
  .about__scroll {
    display: none;
    position: absolute;
    right: 60px;
    left: auto;
    top: 85.21%;
    z-index: 5;
    align-items: center;
    gap: 8px;
    border: 1px solid var(--color-brand-400);
    border-radius: 100px;
    padding: 4px 12px;
    font-family: var(--font-sans);
    font-weight: 400;
    font-size: clamp(11px, 1.4vw, 18px);
    color: var(--color-brand-900);
    white-space: nowrap;
    cursor: default;
  }

  .about__scroll-arrow {
    flex-shrink: 0;
    color: var(--color-brand-900);
  }

  /* ─────────────────────────────────────────────
     YELLOW TICKER
  ───────────────────────────────────────────── */
  .about__ticker {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 60px;
    background-color: var(--color-yellow-500);
    overflow: hidden;
    z-index: 10;
  }

  @keyframes about-ticker {
    0%   { transform: translateX(0); }
    100% { transform: translateX(-50%); }
  }

  .about__ticker-track {
    display: flex;
    align-items: center;
    gap: 64px;
    width: max-content;
    height: 100%;
    animation: about-ticker 130s linear infinite;
    animation-play-state: paused;
    will-change: transform;
    padding: 0 32px;
  }

  .about__ticker-track.ticker-ready {
    animation-play-state: running;
  }
  
  @media (max-width: 768px) {
    .about__ticker-track { gap: 50px; }
  }

  .about__ticker-logo {
    height: 30px;
    width: 120px;
    flex-shrink: 0;
    display: block;
    object-fit: contain;
    object-position: center;
    filter: brightness(0);
  }
  .about__ticker-logo--sq { width: 40px; }

  .about__ticker-logo[src*="delhi"]        { width: 40px;  height: 50px;  }
  .about__ticker-logo[src*="india_govt"]   { height: 48px; width: 100px;  }
  .about__ticker-logo[src*="times_of_india"] { height: 42px; width: 120px; }
  .about__ticker-logo[src*="the_hindu"]    { width: 240px; height: 30px;  }
  .about__ticker-logo[src*="india_today"]  { height: 38px; width: 150px;  }
  .about__ticker-logo[src*="deccan_herald"] { height: 36px; width: 170px; }
  .about__ticker-logo[src*="livemint"]     { height: 70px; width: 70px;   }
  .about__ticker-logo[src*="grihshobha"]   { height: 38px; width: 150px;  }
  .about__ticker-logo[src*="mumbai_mirror"] { height: 36px; width: 150px; }


  /* ─────────────────────────────────────────────
     TABLET  (769 – 1024 px)
  ───────────────────────────────────────────── */
  @media (max-width: 1024px) and (min-width: 769px) {
    .about__badge { font-size: clamp(14px, 2.2vw, 28px); }
  }

  /* Short desktop/laptop screens — nudge badge down so it stays below the main photo */
  @media (max-height: 720px) and (min-width: 769px) {
    .about__badge { top: 79%; }
  }
  @media (max-height: 620px) and (min-width: 769px) {
    .about__badge { top: 83%; }
  }

  /* ─────────────────────────────────────────────
     MOBILE  (≤ 768 px)
     Scene is fixed 402×772 px design space.
     JS scales it to fit any phone viewport.
     All positions are % of 402w × 772h.
  ───────────────────────────────────────────── */
  @media (max-width: 768px) {

    /* Scene: fixed design size, JS applies scale() */
    .about__scene {
      width: 402px;
      height: 772px;
      right: auto;
      bottom: auto;
      top: 0;
      left: 50%;
      transform: translateX(-50%);   /* JS will replace this */
      transform-origin: top center;
    }

    /* ── photos ── */
    .about__photo--main {
      width: 60%;
      max-width: none;
      left: 17.7%;
      top: 7.5%;
    }

    .about__photo--saree {
      width: 21.3%;
      max-width: none;
      left: 3.2%;
      top: 8.5%;
    }

    .about__photo--billboard {
      width: 28.6%;
      max-width: none;
      left: -4.5%;
      top: 20%;
    }

    .about__photo--doctor {
      width: 19.9%;
      max-width: none;
      left: 8.7%;
      top: 34%;
    }

    .about__photo--diving {
      width: 24.4%;
      max-width: none;
      left: 70.2%;
      top: 13%;
    }

    .about__photo--podcast {
      width: 39.6%;
      max-width: none;
      left: 64.7%;
      top: 29.5%;
    }

    /* ── badge: fixed px within design canvas ── */
    .about__badge {
      left: 47.5%;
      top: 37%;
      transform: translateX(-50%);
      font-size: 16px;
    }

    /* ── stars ── */
    .about__star--1 { width: 32px; left: 13.9%; top: 6.5%; }
    .about__star--2 { width: 36px; left: 65.2%; top: 26.5%; }
    .about__star--3 { display: block; width: 32px; left: 21.4%; top: 38.5%; }
    .about__star--4 { display: none; }

    .about__highlight-bar { display: none; }

    /* ── headlines: fixed sizes (scale handles visual resize) ── */
    .about__headline {
      font-size: 48px;
      right: auto;
      text-align: center;
      transform: translateX(-50%);
    }
    .about__headline--1 { left: 50%; top: 52%; }
    .about__headline--2 { left: 50%; top: 59%; }

    /* ── pill ── */
    .about__pill-wrap {
      left: 50%;
      top: 65%;
      transform: translateX(-50%) rotate(-5.99deg);
    }
    .about__pill-bakchodi {
      font-size: 20px;
    }

    /* ── bio ── */
    .about__bio-desktop { display: none; }

    .about__bio-mobile {
      display: block;
      position: absolute;
      left: 50%;
      top: 72%;
      width: 95%;
      transform: translateX(-50%);
      font-family: var(--font-sans);
      font-weight: 400;
      font-size: 20px;
      line-height: 1.2;
      color: var(--color-brand-900);
      text-align: center;
      margin: 0;
      z-index: 5;
    }
    .about__bio-mobile--english { display: none; }
    :global([data-lang="english"]) .about__bio-mobile--english  { display: block; }
    :global([data-lang="english"]) .about__bio-mobile--hinglish { display: none; }

    /* ── CTA ── */
    .about__cta-wrap {
      right: auto;
      left: 50%;
      top: 85%;
      transform: translateX(-50%);
    }
    .about__cta { font-size: 19px; }

    /* ── scroll indicator ── */
    .about__scroll {
      display: none;
    }
  }
</style>

<script>
  /* ── Ticker ── */
  const track = document.querySelector<HTMLElement>('.about__ticker-track');
  if (track) {
    const imgs = Array.from(track.querySelectorAll<HTMLImageElement>('img'));
    const total = imgs.length;
    if (total === 0) {
      track.classList.add('ticker-ready');
    } else {
      let loaded = 0;
      function onLoad() {
        loaded++;
        if (loaded >= total) track.classList.add('ticker-ready');
      }
      imgs.forEach(img => {
        if (img.complete) onLoad();
        else { img.addEventListener('load', onLoad, { once: true }); img.addEventListener('error', onLoad, { once: true }); }
      });
    }
  }

  /* ── Mobile scene scaling ──
     Fits the fixed 402×772 design canvas into any phone screen
     so all elements (photos, text, stars, pill, CTA) scale together. */
  const DESIGN_W = 402;
  const DESIGN_H = 772;
  const TICKER_H = 60;
  const SCALE_MIN = 0.62;   // floor: keeps text readable on very small phones
  const SAFE_PAD = 8;       // px of breathing room against navbar + ticker edges

  function applyAboutScale() {
    const scene = document.querySelector<HTMLElement>('.about__scene');
    if (!scene) return;

    if (window.innerWidth > 768) {
      scene.style.transform = '';
      scene.style.top = '';
      return;
    }

    const vw = window.innerWidth;
    // Use the section's own height (matches 100svh) instead of window.innerHeight
    // so iOS address-bar show/hide never mis-positions the scene.
    const sectionH = (scene.closest<HTMLElement>('section')?.offsetHeight) ?? window.innerHeight;
    const navbarH  = (document.querySelector<HTMLElement>('.navbar')?.offsetHeight) ?? 56;
    const availH   = sectionH - TICKER_H - navbarH - SAFE_PAD * 2;
    const scale    = Math.max(SCALE_MIN, Math.min(vw / DESIGN_W, availH / DESIGN_H));
    const scaledH  = DESIGN_H * scale;
    // Centre the scene in the space between navbar and ticker
    const topOffset = navbarH + SAFE_PAD + Math.max(0, (availH - scaledH) / 2);

    scene.style.top = `${topOffset}px`;
    scene.style.transform = `translateX(-50%) scale(${scale})`;
  }

  // Defer until layout is stable — on first visit svh units may not be
  // resolved yet when the script runs, causing sectionH to read as 0.
  if (document.readyState === 'complete') {
    applyAboutScale();
  } else {
    window.addEventListener('load', applyAboutScale, { once: true });
  }
  window.addEventListener('resize', applyAboutScale);
</script>
